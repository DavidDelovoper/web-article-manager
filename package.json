{
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  
  "scripts": {
      "install:all": "cd backend && npm install && cd ../frontend && npm install",
      "start:all": "concurrently \"cd backend && node src/index.js\" \"cd frontend && npm run dev\""
    }
}
